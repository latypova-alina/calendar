
<% @meetings = Meeting.all if @meetings == nil %>

<%= month_calendar events: @meetings do |date, meetings| %>
  
  <%= date %>
  
  <% meetings.each do |meeting| %>
    <div>
      <%= link_to "#{meeting.name}", edit_meeting_path(meeting) %>
    </div>
  <% end %>
<% end %>

 

